# 7+1+BONUS - TECHNICAL REFERENCE
## COMPILATION ORDERS
### Prerequisites
- Java JDK
- Apache Maven
- `JAVA_HOME` environment variable properly set

### Compilation
From the root directory of the project (where the `pom.xml` file is located), run:
```bash
mvn clean compile
